// @generated by `cargo xtask shaders` from shaders/post.slang — do not edit.
// source-hash: sha256:22535107693547832aa163c2a1376fc4fa78a6f6577860ca3d708959ffda8ccc generator: v5
//
// Layout facts reflected from Slang, frozen as compile-time assertions:
// a shader/CPU struct mismatch is a build error (PLAN.md §4.4).

/// Push-constant block `PostPush` — std430 layout, 24 bytes.
#[repr(C)]
#[derive(Clone, Copy, Debug, bytemuck::Pod, bytemuck::Zeroable)]
pub struct PostPush {
    /// offset 0
    pub texture_index: u32,
    /// offset 4
    pub sampler_index: u32,
    /// offset 8
    pub exposure: f32,
    /// offset 12
    pub aa: u32,
    /// offset 16
    pub texel: [f32; 2],
}

impl PostPush {
    /// Construct with every padding byte zeroed.
    pub fn new(
        texture_index: u32,
        sampler_index: u32,
        exposure: f32,
        aa: u32,
        texel: [f32; 2],
    ) -> Self {
        Self {
            texture_index,
            sampler_index,
            exposure,
            aa,
            texel,
        }
    }
}

const _: () = {
    assert!(core::mem::size_of::<PostPush>() == 24);
    assert!(core::mem::offset_of!(PostPush, texture_index) == 0);
    assert!(core::mem::offset_of!(PostPush, sampler_index) == 4);
    assert!(core::mem::offset_of!(PostPush, exposure) == 8);
    assert!(core::mem::offset_of!(PostPush, aa) == 12);
    assert!(core::mem::offset_of!(PostPush, texel) == 16);
};

/// SPIR-V for entry point `vs_main` (vertex).
pub static VS_MAIN_SPIRV: &[u8] = include_bytes!("post.vs_main.spv");
/// The `OpEntryPoint` name inside [`VS_MAIN_SPIRV`].
pub const VS_MAIN_ENTRY: &str = "main";

/// SPIR-V for entry point `fs_main` (fragment).
pub static FS_MAIN_SPIRV: &[u8] = include_bytes!("post.fs_main.spv");
/// The `OpEntryPoint` name inside [`FS_MAIN_SPIRV`].
pub const FS_MAIN_ENTRY: &str = "main";

/// SPIR-V for entry point `fs_downsample` (fragment).
pub static FS_DOWNSAMPLE_SPIRV: &[u8] = include_bytes!("post.fs_downsample.spv");
/// The `OpEntryPoint` name inside [`FS_DOWNSAMPLE_SPIRV`].
pub const FS_DOWNSAMPLE_ENTRY: &str = "main";
